<h1 class="games-index-h1"><%= current_user.company %></h1>
<% if @games.present? %>
<% @games.each do |g|  %>
<div class="wrapper">
    <div class="one"><%= display_picture g %></div>
    <div class="two">
        <h3><%= link_to g.title, game_path(g), class: "game-title" %></h3>
        <% g.tags.each do |t| %>

          <span class="tag-boxes text-center">
              <%= t.name %>
          </span>

        <% end %>
        <p class="clearfix"></p s>
            <p class="game-aasm_state"><%= g.aasm_state %></p>
        <p class="pull-left" style="margin-right:20px; margin-bottom:20px;"><%=  link_to "Feedback", game_path(g), :class=>"btn btn-default" %>
            <%= link_to "Update", edit_game_path(g), :class=>"btn btn-default" %>
            <%= link_to "Controller Keys", new_game_key_path(g), :class=>"btn btn-default" %>
            <%= link_to "Remove", game_path(g),
                                 method: :delete,
                                 data: {confirm: 'Are you sure?'}, :class=>"btn btn-default" %></p>
        <br>
        <br>
    </div>
</div>
<% end %>
<% else %>
<div class="box">
    <p>There are no matches for your search</p>
</div>
<% end %>
<%= paginate @games %>
